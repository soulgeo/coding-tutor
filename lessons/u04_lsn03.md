---
title: List Methods
pretypedCode: |
  todo = ["Sleep", "Code"]
  # Add "Eat" to the list
  todo.append("Eat")
  print(todo)
expectedOutput: "['Sleep', 'Code']"
successMessage: "skills.append('Lists')"
---

## List Methods

Lists have special built-in tools called **methods**. You use them by putting a dot `.` after the list name.

- `.append(item)`: Adds an item to the **end** of the list.
- `.pop()`: Removes the **last** item from the list.

---

### Your Task
1. The code adds `"Eat"` to the list.
2. On a new line before the print, use `todo.pop()` to remove the last item.
3. Click **Submit**. The list should go back to `['Sleep', 'Code']`.
