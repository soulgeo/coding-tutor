---
title: Mini Database
pretypedCode: |
  users = ["alice", "bob", "charlie"]
  
  def check_user(name):
      if name in users:
          return "Access Granted"
      return "Access Denied"
  
  print(check_user("bob"))
expectedOutput: Access Granted
---

## Mini Database

We can combine lists and functions to create a simple security system.

---

### Your Task
1. Change the call to `check_user("dave")`.
2. Click **Submit**.
