---
title: For Loops
pretypedCode: |
  # A for loop runs once for every item in a sequence
  for letter in "Python":
      print(letter)
expectedOutput: |
  C
  o
  d
  e
successMessage: "For sure!"
---

## The "for" Loop

While `while` loops run based on a condition, `for` loops run through a sequence (like the letters in a word) and stop automatically when they reach the end.

```python
for x in "Hi":
    print(x)
```
This is often safer and cleaner than a `while` loop!

---

### Your Task
1. Change the string `"Python"` to `"Code"`.
2. Click **Submit** to see the word printed letter by letter.
