---
title: List Filtering
pretypedCode: |
  def get_positives(numbers):
      positives = []
      for n in numbers:
          if n > 0:
              positives.append(n)
      return positives
  
  my_list = [-5, 10, -2, 25, 0]
  print(get_positives(my_list))
expectedOutput: "[10, 25]"
---

## List Filtering

A common task is to go through a list and pick out only the items that meet a certain condition.

In this project, we take a list of numbers and create a new list that only contains the positive ones.

---

### Your Task
1. Look at the code. It uses a loop and an `if` statement to filter the list.
2. Click **Submit**.
