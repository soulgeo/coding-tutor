---
title: Elif Statements
pretypedCode: |
  temp = 25
  if temp > 30:
      print("Hot")
  elif temp > 20:
      print("Warm")
  else:
      print("Cold")
expectedOutput: "Hot"
successMessage: "Hot, hot, hot!!"
hint: "Set `temp = 35` at the very beginning to trigger the first `if` block."
---

## Many Choices

Sometimes you have more than two options. For that, we use `elif` (short for "else if").

You can have as many `elif` blocks as you want between the `if` and the `else`. Python checks them one by one from top to bottom and stops at the **first** one that is True.

---

### Your Task
1. Change `temp` to `15`.
2. Click **Run**. Python will skip the `if` and `elif` and run the `else` block.
3. Change `temp` to `35` and Click **Submit**.
