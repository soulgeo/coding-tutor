---
title: Function Parameters
pretypedCode: |
  def greet(name):
      print("Hi " + name)
  
  greet("Alice")
expectedOutput: Hi Programmer
successMessage: "Passing data like a pro."
hint: "Change the string `\"Alice\"` to `\"Programmer\"` in the `greet()` call."
---

## Giving Data (Parameters)

Functions become much more useful when you can give them data to work with. We do this using **Parameters**.

Think of a parameter like a variable that only exists inside the function.

```python
def square(number):
    print(number * number)
```

---

### Your Task
1. Change the call `greet("Alice")` to `greet("Programmer")`.
2. Click **Submit**.
