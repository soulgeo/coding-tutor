---
title: Stop! (Break)
pretypedCode: |
  for i in range(1, 100):
      print(i)
      if i == 5:
          break
expectedOutput: |
  1
  2
  3
  4
  5
---

## Stop! (Break)

Sometimes you want to stop a loop immediately, even if it hasn't finished its sequence. For that, we use the `break` command.

In the example, the loop wants to go up to 100, but we tell it to `break` as soon as it hits 5.

---

### Your Task
1. Look at the code. It stops at 5.
2. Change the `if` statement so it breaks when `i` reaches `10`.
3. Click **Submit**.
