---
title: Combining Logic
pretypedCode: |
  has_key = True
  is_open = False
  
  if has_key and is_open:
      print("You may enter!")
  else:
      print("The door is stuck.")
expectedOutput: The door is stuck.
---

## Combining Logic

You can check multiple conditions at once using `and` and `or`.

- `and`: Both conditions must be True.
- `or`: At least one condition must be True.
- `not`: Flips the boolean (`not True` becomes `False`).

---

### Your Task
1. Currently, the door is stuck because `is_open` is `False`.
2. Change `is_open` to `True`.
3. Click **Submit**. Since both are now `True`, the first message will print.
