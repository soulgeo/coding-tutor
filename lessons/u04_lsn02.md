---
title: Indexing
pretypedCode: |
  colors = ["Red", "Green", "Blue"]
  # Print "Green" by using its index
  print(colors[0])
expectedOutput: Green
---

## Indexing

To get a single item out of a list, we use its **index** (its position).

**Important:** Python starts counting at **0**.
- Index 0 is the 1st item.
- Index 1 is the 2nd item.
- Index 2 is the 3rd item.

---

### Your Task
1. The code currently prints `colors[0]`, which is `"Red"`.
2. Change the index to `1` to print `"Green"`.
3. Click **Submit**.
