---
title: Loops and Lists
pretypedCode: |
  shopping_list = ["Milk", "Eggs", "Bread"]
  for item in shopping_list:
      print("Buy " + item)
expectedOutput: |
  Buy Milk
  Buy Eggs
  Buy Bread
---

## Loops and Lists

`for` loops and lists are best friends. You can use a loop to go through a list one item at a time.

```python
for item in my_list:
    print(item)
```

Python automatically assigns the current item to the variable `item` and runs the code inside for every item in the list.

---

### Your Task
1. Change the message inside the loop to `"Get " + item`.
2. Click **Submit**.
