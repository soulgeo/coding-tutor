---
title: List Length
pretypedCode: |
  items = [10, 20, 30, 40, 50]
  # Use len() to get the size
  print(items)
expectedOutput: "5"
successMessage: "len(progress) is growing every day."
hint: "Change the print statement to: `print(len(items))`"
---

## How Long is it?

You can find out how many items are in a list using the `len()` function.

```python
my_list = ["A", "B"]
print(len(my_list)) # Prints 2
```

---

### Your Task
1. Wrap the `items` variable inside `len()` in the print statement: `print(len(items))`.
2. Click **Submit**.
