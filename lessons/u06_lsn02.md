---
title: String Processing
pretypedCode: |
  def shout(text):
      return text.upper() + "!!!"
  
  print(shout("hello"))
expectedOutput: HELLO!!!
---

## String Processing

Python strings have many useful methods, like `.upper()` which turns everything into capital letters.

In this project, we'll create a "shouting" function that caps the text and adds exclamation marks.

---

### Your Task
1. Change the call `shout("hello")` to `shout("python is great")`.
2. Click **Submit**.
