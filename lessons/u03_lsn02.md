---
title: The Even Stevener
pretypedCode: |
  num = 2
  # Use a while loop to print even numbers from 2 to 10
expectedOutput: |
  2
  4
  6
  8
  10
hint: "Your loop should look like this:\n\n```python\nwhile num <= 10:\n    print(num)\n    num = num + 2\n```"
---

## Counting Up

Loops are great for patterns. Instead of just adding 1, you can increment your variable by any amount to skip numbers.

```python
x = 0
while x < 100:
    print(x)
    x = x + 10 # Counts by 10s!
```

---

### Your Task
1. Write a `while` loop that runs as long as `num <= 10`.
2. Inside the loop, `print(num)`.
3. Update `num` so it increases by **2** every time.
4. Click **Submit** to see the even numbers 2 through 10.
