---
title: Inventory Checker
pretypedCode: |
  inventory = ["Sword", "Shield", "Potion"]
  item = "Food"
  
  # Check if item is in inventory. If not, add it!
  
  print(inventory)
expectedOutput: "['Sword', 'Shield', 'Potion', 'Food']"
successMessage: "Searching for success... Found!"
hint: "Your code should look like this:\n\n```python\nif item not in inventory:\n    inventory.append(item)\n```"
---

## Searching

You can use `if item in list:` to check for data. This is often used to prevent duplicates or to decide what to do next in a program.

```python
if "Apple" not in basket:
    basket.append("Apple")
```

---

### Your Task
1. Write an `if` statement to check if the `item` variable is **NOT** in the `inventory` list.
2. If it is missing, use `.append(item)` to add it.
3. Click **Submit**. Since `"Food"` is missing, your code should add it to the list.
