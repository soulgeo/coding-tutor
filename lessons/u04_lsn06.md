---
title: Searching
pretypedCode: |
  guests = ["Alice", "Bob", "Charlie"]
  name = "Bob"
  
  if name in guests:
      print("Welcome!")
  else:
      print("Not on the list.")
expectedOutput: Welcome!
---

## Searching

You can check if an item exists in a list using the `in` keyword. It returns `True` if the item is there and `False` if it isn't.

```python
if "Apple" in fruits:
    print("Found it!")
```

---

### Your Task
1. Change `name` to `"Dave"`.
2. Click **Submit** to see the `else` block run.
