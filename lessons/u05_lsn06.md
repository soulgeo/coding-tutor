---
title: Default Values
pretypedCode: |
  def greet(name="Friend"):
      print("Hello " + name)
  
  greet("Alice")
  greet()
expectedOutput: |
  Hello Alice
  Hello Friend
---

## Default Values

You can make parameters optional by giving them a **default value**. If you call the function without an argument, it will use the default.

```python
def power(num, exponent=2):
    return num ** exponent
```

---

### Your Task
1. Change the default value from `"Friend"` to `"User"`.
2. Click **Submit**.
