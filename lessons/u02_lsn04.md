---
title: Else Statements
pretypedCode: |
  lives = 0
  if lives > 0:
      print("Keep playing!")
  else:
      print("Game Over")
expectedOutput: "Keep playing!"
successMessage: "You took the else path to victory!"
hint: "Simply change `lives = 0` to `lives = 3` at the top of the code."
---

## The "else" Path

What if you want to do one thing when a condition is True, and a **different** thing when it is False? We use `else`.

The `else` block runs only if the `if` condition above it is False. It doesn't need its own condition.

---

### Your Task
1. Look at the code. Since `lives` is `0`, the `else` block will run.
2. Change `lives` to `3`.
3. Click **Submit** to see the `if` block run instead.
