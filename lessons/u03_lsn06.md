---
title: Continue Statement
pretypedCode: |
  for i in range(1, 6):
      if i == 3:
          continue
      print(i)
expectedOutput: |
  1
  3
  4
  5
successMessage: "Continue being amazing!"
hint: "Change `if i == 3:` to `if i == 2:` to skip the number 2."
---

## Skipping Items

While `break` stops the whole loop, `continue` just skips the **rest of the current turn** and jumps back to the top for the next one.

It's like saying: "If this happens, skip this one item and keep going."

---

### Your Task
1. Look at the output. Notice that `3` is missing!
2. Change the `if` statement to skip the number `2` instead.
3. Click **Submit**.
