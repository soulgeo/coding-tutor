---
title: Multiple Parameters
pretypedCode: |
  def multiply(a, b):
      return a * b
  
  print(multiply(5, 5))
expectedOutput: 25
---

## Multiple Parameters

Functions can take as many parameters as you need. Just separate them with commas.

```python
def describe_pet(name, animal):
    print("I have a " + animal + " named " + name)
```

---

### Your Task
1. Change the `multiply` call to use `10` and `10`.
2. Click **Submit** to see `100`.
