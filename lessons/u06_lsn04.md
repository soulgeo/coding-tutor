---
title: Grade Calculator
pretypedCode: |
  def get_grade(score):
      if score >= 90:
          return "A"
      elif score >= 80:
          return "B"
      else:
          return "F"
  
  print(get_grade(85))
expectedOutput: B
---

## Grade Calculator

Logic is the core of most programs. Here, we use `if` and `elif` to determine a letter grade based on a numerical score.

---

### Your Task
1. Change the input score from `85` to `95`.
2. Click **Submit**.
