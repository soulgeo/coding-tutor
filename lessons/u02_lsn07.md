---
title: Unit 2 Recap
pretypedCode: |
  number = -5
  # Write your analysis logic below
expectedOutput: Negative
successMessage: "if unit == 2: print('Graduate')"
hint: "Structure your code like this:\n\n```python\nif number > 0:\n    print(\"Positive\")\nelif number < 0:\n    print(\"Negative\")\nelse:\n    print(\"Zero\")\n```"
---

## Unit 2 Recap

You've learned how to give your program a "brain" using Booleans and Conditionals. Now it's time to put it all together.

---

### Your Final Unit 2 Challenge: Number Analyzer
Write a program that analyzes the `number` variable:
1. If the number is **greater than 0**, print `"Positive"`.
2. If the number is **less than 0**, print `"Negative"`.
3. If the number is **exactly 0**, print `"Zero"`.

*Hint: Use `if`, `elif`, and `else`!*

Click **Submit** once your code handles all three cases correctly.
