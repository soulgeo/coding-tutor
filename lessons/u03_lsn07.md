---
title: Unit 3 Recap
pretypedCode: |
  total = 0
  # Write a for loop to sum numbers 1 to 5
  
  print(total)
expectedOutput: "15"
successMessage: "total_score = sum(unit3_lessons)"
hint: "Use a loop with `range(1, 6)` and add each number to the total:\n\n```python\nfor i in range(1, 6):\n    total = total + i\n```"
---

## Unit 3 Recap

You've mastered automation! One of the most common uses for loops is to "accumulate" a value, like adding up a total score.

```python
total = 0
for n in range(5):
    total = total + n
```

---

### Your Final Unit 3 Challenge: The Summator
1. Write a `for` loop using `range(1, 6)` (this will give you numbers 1, 2, 3, 4, 5).
2. Inside the loop, add the current number to the `total` variable.
3. Click **Submit**. The final print should show `15`.
