---
title: Return Statement
pretypedCode: |
  def add_five(num):
      return num + 5
  
  result = add_five(10)
  print(result)
expectedOutput: "15"
successMessage: "return 'Mission Accomplished'"
---

## Getting Results (Return)

So far, our functions have just printed things. But often, you want a function to calculate something and **give it back** to the rest of your program. We use the `return` keyword for this.

Once a function `return`s a value, it stops immediately.

---

### Your Task
1. Change the call `add_five(10)` to `add_five(20)`.
2. Click **Submit**. The result should be `25`.
